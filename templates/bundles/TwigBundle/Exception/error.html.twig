{% extends 'base.html.twig' %}{% block title %}Átmeneti hiba – KórházKapu{% endblock %}{% block body %}<main id="main-content" class="error-page"><span aria-hidden="true">!</span><h1>Most nem sikerült betölteni az oldalt</h1><p>Átmeneti technikai hiba történt. Kérjük, próbálja meg néhány perc múlva újra.</p><div>{% if app.request.attributes.get('institutionSlug') %}<a class="button" href="{{ path('tenant_home',{institutionSlug:app.request.attributes.get('institutionSlug')}) }}">Intézményi főoldal</a>{% else %}<a class="button" href="{{ path('platform_home') }}">Főoldal</a>{% endif %}</div></main>{% endblock %}
